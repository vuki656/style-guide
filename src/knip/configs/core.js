/** @type {import("knip").KnipConfig} */
const config = {
    ignore: [],
    ignoreDependencies: [
        "cspell",
        "eslint",
        "prettier",
        "stylelint",
        "stylelint-no-unused-selectors",
        "stylelint-order",
    ],
}

export default config
